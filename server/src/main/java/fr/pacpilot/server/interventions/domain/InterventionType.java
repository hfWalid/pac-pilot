package fr.pacpilot.server.interventions.domain;

/** The kinds of visit the app owns ({@code CLAUDE.md} §14). */
public enum InterventionType {
    PRE_VISIT,
    INSTALL,
    MAINTENANCE,
    SAV
}
